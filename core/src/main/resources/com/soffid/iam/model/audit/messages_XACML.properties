Q=User ${user} at service ${application} has requested access to vault folder ${account}
D=Denied access to account ${account} at ${database} for service ${application} and user ${author}
A=Granted access to account ${account} at ${database} for service ${application} and user ${author}
